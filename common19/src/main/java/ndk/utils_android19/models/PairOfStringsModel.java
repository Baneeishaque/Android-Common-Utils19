package ndk.utils_android19.models;

import androidx.core.util.Pair;

public class PairOfStringsModel extends Pair<String, String> {

    /**
     * Constructor for a Pair.
     *
     * @param first  the first object in the Pair
     * @param second the second object in the pair
     */
    public PairOfStringsModel(String first, String second) {
        super(first, second);
    }
}
